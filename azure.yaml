# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: fvsd-insights
metadata:
  template: fvsd-insights@0.1.0

services:
  web:
    project: ./src/FVSDNexus.Api
    host: appservice
    language: dotnet
